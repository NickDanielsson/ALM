name: Test workflow

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo Hej hopp!

      - name: Step 2
        run: echo Hej då!